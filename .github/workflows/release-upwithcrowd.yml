name: Playwright Tests
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [ upwithcrowd ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: self-hosted
    steps:
    - run: echo "🎉 Going Live!"
    - name: Release upwithcrowd
      run: ssh ubuntu "cd ~/frontend/prod_upwithcrowd && chmod 777 publish.sh && ./publish.sh 3599 upwithcrowd_prod"